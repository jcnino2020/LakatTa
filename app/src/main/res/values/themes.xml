<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.LakatTa" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Sets the background color of the status bar -->
        <item name="android:statusBarColor">@color/status_bar_color</item>

        <!-- Makes the status bar icons (time, battery, etc.) dark for visibility on a light background -->
        <item name="android:windowLightStatusBar">true</item>

    </style>
    <style name="Theme.LakatTa" parent="Base.Theme.LakatTa" />

    <style name="ProfileMenuItem" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="ProfileMenuDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">#E0E0E0</item>
    </style>

    <style name="FilterChip" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:background">@drawable/chip_background</item>
        <item name="android:textColor">#424242</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>